name: Deploy Hair Salon

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Sync Files to Server
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
          ARGS: "-rlgoDzvc --delete"
          SOURCE: "./"
          REMOTE_HOST: ${{ secrets.SERVER_HOST }}
          REMOTE_USER: ${{ secrets.SERVER_USER }}
          TARGET: "/var/www/hair_salon" 
          EXCLUDE: "/node_modules/, /.git/, /.github/, /.env" 

      - name: Execute Remote SSH Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            [ -f ~/.bashrc ] && source ~/.bashrc
            [ -f ~/.profile ] && source ~/.profile
            cd /var/www/hair_salon || exit 1
            npm install --omit=dev
            if pm2 list | grep -q "hair-salon-frontend"; then
              echo "A frontend már fut, újraindítás..."
              pm2 restart hair-salon-frontend --update-env
            else
              echo "A frontend még nem fut, első indítás..."
              pm2 start index.js --name "hair-salon-frontend"
            fi
            if pm2 list | grep -q "hair-salon-api"; then
              echo "Az API már fut, újraindítás..."
              pm2 restart hair-salon-api --update-env
            else
              echo "Az API még nem fut, első indítás..."
              pm2 start api.js --name "hair-salon-api"
            fi
            pm2 save